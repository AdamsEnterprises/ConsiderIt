%p Enter your email address below to receive instructions for resetting your password.

= form_for(resource, :as => resource_name, :url => password_path(resource_name)) do |f|
  = f.label :email
  = f.email_field :email

  %ul.link_list
    %li
      = f.submit "Send instructions"
    %li
      = link_to "Return to login", mobile_user_path
    %li
      = link_to "Didn't receive confirmation instructions?", mobile_confirm_resend_path
