%p What's your current opinion of #{@option.reference}?
= form_for @position, :url => mobile_navigate_position_update_path do |f|
  = render :partial => "mobile/partials/position_select", :locals => { :form => f }
  - if not @required_update
    = f.submit "Update Position", :name => "button[update_position]"
  = render :partial => "mobile/partials/nav", :locals => { :id => "nav_bottom", :navigation => @navigation, :include_header => true, :form => f }

